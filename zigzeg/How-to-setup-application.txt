1) create a database and update you config/database.yml

2) RAILS_ENV=production rake db:migrate
  - dump the database structure

3) RAILS_ENV=production rake setup:default_data
  - load default users, listing_types, user_types into the database

  - update the activated_at column
  rake migration:update_activated_date 
4) OPTIONAL 
  RAILS_ENV=production rake setup:load_test_data
  - load sample data into the database
5) dump the raw countries sql to the database
  mysql -u root -p zigzeg_production < countries-regions-cities.sql
6) LOAD countries and cities
   RAILS_ENV=production rake setup:load_cities_countries
  - load sample data into the database



run the mailers in the background
god -c god/mailers.god
