<div class="grid_0 align_right">
  <% unless current_user.nil? %>   
		<a href="#accountSettings" id="signUpLink" name="menuM" class="logged noDecor settings"><div><%= truncate(current_user.name_or_email, :length => 25, :omission => "...") %></div></a>
    <div class="separator"></div>
		<% if @unread_updates.length > 0 %>
    <a class="logged updates bold" name="menuM" href="#updatesListing"><%= @unread_updates.length %></a>
    <% else %>
		<a class="logged updates bold noHover noCursor"><%= @unread_updates.length %></a>
    <% end %>
    
<<<<<<< HEAD
		<div class="separator"></div>&nbsp;&nbsp;

    <% if current_user.is_admin?%>
      <a id="total_messages" class="radius5 noDecor cursor <%= 'newMsg' unless @unread_messages.length == 0 %>" href="/admin/messages"><span><%= @unread_messages.length%></span> Message<% if @unread_messages.length > 1 %>s<% end %></a>
    <% elsif current_user.is_advertiser?%>
      <a id="total_messages" class="radius5 noDecor cursor <%= 'newMsg' unless @unread_messages.length == 0 %>" href="/business/messages"><span><%= @unread_messages.length %></span> Message<% if @unread_messages.length > 1 %>s<% end %></a>
    <% else %>
      <a id="total_messages" class="radius5 noDecor cursor <%= 'newMsg' unless @unread_messages.length == 0 %>" href="/account/messages"><span><%= @unread_messages.length %></span> Message<% if @unread_messages.length > 1 %>s<% end %></a>
    <% end %>
=======
		<div class="separator"></div>
      <% if current_user.is_admin?%>
        <a class="radius5 noDecor cursor <%= 'newMsg' unless @unread_messages.length == 0 %>" href="/admin/messages"><%= @unread_messages.length %>  <%= "New"  unless @unread_messages.nil? %> Message<% if @unread_messages.length > 1 %>s<% end %></a>
      <% elsif current_user.is_advertiser?%>
        <a class="radius5 noDecor cursor <%= 'newMsg' unless @unread_messages.length == 0 %>" href="/business/messages"><%= @unread_messages.length %> <%= "New"  unless @unread_messages.nil? %> Message<% if @unread_messages.length > 1 %>s<% end %></a>
      <% else %>
        <a class="radius5 noDecor cursor <%= 'newMsg' unless @unread_messages.length == 0 %>" href="/account/messages"><%= @unread_messages.length %> <%= "New"  unless @unread_messages.nil? %> Message<% if @unread_messages.length > 1 %>s<% end %></a>
      <% end %>
>>>>>>> 552fb8504cfe6060065c49840d42a8cdf2182145
    
  <% end %>
</div>
