#SubdomainFu.tld_size = 1 # sets for current environment
SubdomainFu.tld_sizes = {:development => 1,
                         :test => 0,
                         :production => 2} # set all at once (also the defaults)

SubdomainFu.mirrors = %w(www a) # Defaults to %w(www)
